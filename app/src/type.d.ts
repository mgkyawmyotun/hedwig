/** need to improve */
// type RequestItemType = 'name' | 'method' | 'options' | 'url' | 'params';

type CollectionType = {
  name: string;
  items: [];
};
type CollectionState = {
  collections: CollectionsType[];
};
